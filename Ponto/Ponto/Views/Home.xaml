<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             NavigationPage.HasNavigationBar="False"
             x:Class="Ponto.Views.Home">
    <ContentPage.Content>
        <!--Gradiente de fundo-->
        <Grid RowDefinitions="20*, 80*">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,1"
                             EndPoint="0,0">
                    <GradientStop Color="White"
                          Offset="0.2"/>
                    <GradientStop Color="CadetBlue"
                          Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <!--Frame Saldo-->
            <Frame Margin="20" CornerRadius="30">
                <Label Text="Saldo"/>
            </Frame>

            <!--"frame" branco-->
            <BoxView CornerRadius="30,30,0,0" Grid.Row="1" BackgroundColor="White" Margin="20,0,20,0"/>


            <Grid Grid.Row="1" RowDefinitions="auto" ColumnDefinitions="40,*,40">
                <StackLayout Grid.Row="1" Grid.Column="1">
                    <!--<ListView>
                        <ListView.ItemTemplate>
                            <DataTemplate>-->
                                <Frame CornerRadius="30">
                                    <Grid ColumnDefinitions="70*,30*" RowDefinitions="auto,1, auto ,auto">
                                        <BoxView Color="Gray" HeightRequest="1" Grid.ColumnSpan="2" Grid.Row="1"/>
                                        <!--Esquerda-->
                                        <Label Text="06/06/2022"/>
                                        <Label Text="Terça feira" Grid.Row="2"/>
                                        <Label Text="Tempo de Jornada:   06:20" Grid.Row="3"/>
                                        <!--Direita-->
                                        <Label Text="Finalizado" Grid.Column="1"/>
                                        <Label Text="Hora Extra 00:20" Grid.Column="1" Grid.Row="3"/>
                                    </Grid>
                                </Frame>
                            <!--</DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>-->
                    <Button Text="Ponto" VerticalOptions="End"/>
                </StackLayout>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>