<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Ponto.ViewModels"
             x:Class="Ponto.Views.Login">

    <ContentPage.BindingContext>
        <views:LoginViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid RowDefinitions="40*, 80*">
            <Grid.Background>
                <LinearGradientBrush StartPoint="1,1"
                             EndPoint="0,0">
                    <GradientStop Color="White"
                          Offset="0.1"/>
                    <GradientStop Color="CadetBlue"
                          Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <BoxView CornerRadius="30,30,0,0" Grid.Row="1" BackgroundColor="White" Margin="20,0,20,0"/>

            <Grid Grid.Row="1" RowDefinitions="auto, auto, auto, auto, auto, auto, auto" ColumnDefinitions="40,*,40">
                <Label Text="Bem Vindo" FontSize="40" TextColor="Black" Grid.Column="1" HorizontalTextAlignment="Center"/>
                <Label Text="Seja bem vindo a plataforma da Gama!" FontSize="15" TextColor="Black" Grid.Column="1" Grid.Row="1" HorizontalTextAlignment="Center"/>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>